
@model IEnumerable<string>

<div class="bg-info">
    This is the message form the strongly typed partial partial view.
    <ul>
        @foreach (string str in Model)
        {
            <li>@str</li>
        }
        
    </ul>
</div>